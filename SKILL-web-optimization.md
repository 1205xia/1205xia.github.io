# 网页优化 Skill

## 触发与执行顺序
当用户要求创建、修改、优化个人主页、作品集、布局、响应式或动效时，先读取本 Skill。

不得在个人信息不完整时直接编造内容。第一步必须向用户提问并等待回答；收集完信息后，再复述页面结构、视觉方向、动效时间线和公开内容，得到确认后才开始写代码。

## 第一步：必须向用户确认
1. 姓名、页面称呼、英文名或英文缩写。
2. 网站目的：个人介绍、作品集、求职展示、项目归档或其他用途。
3. 访客看完后要记住的三个关键词、成果或特点。
4. 左侧栏或主导航栏目，以及每个栏目要展示的工作内容。
5. 每个真实项目的背景、本人负责的工作、工具、产出和可验证数据。
6. 技能分为熟练、正在学习、探索中，避免夸大。
7. 是否需要记账、词汇、任务、知识库或其他工作台功能。
8. 可公开的邮箱、社交账号、电话、二维码或其他联系方式。
9. 喜欢与不喜欢的颜色、字体、图片和整体气质。
10. 是否需要图片、视频、Lottie 或其他动画；素材由用户提供还是允许开源素材。
11. 单页滚动还是首页封面点击后进入独立栏目页面，以及返回方式。
12. 电脑、手机、微信内置浏览器、低网速和禁用自动播放等适配要求。

## 视觉方向
- 默认采用轻盈、优雅、编辑感的视觉：浅色底、低饱和配色、细衬线标题和清晰正文。
- 文字必须有足够对比度；使用规整网格，左右边界对齐，禁止阶梯式错位和大面积空白。
- 内容页面充分利用桌面首屏空间，字号与画布匹配，不能做成窄小的手机比例。
- 动画必须是肉眼可见的真实运动，不得只改变背景颜色。

## 开场封面与入场动画
当前正式入口：花园背景中的黑色门视频，点击后进入个人主页。
1. 保留 `src/main.tsx` 中的 `videoUrl` 入口视频和 `.gate-video` 结构，不替换为纯 CSS 门。
2. 入口视频上的黑色门是当前网站确认的正式门；禁止误恢复纯 CSS 黑门、透明门或左右双开门。
3. 不要额外叠加门把手、星星、蝴蝶、流沙、遮罩或光圈，除非用户重新明确要求。
4. 点击区域只负责触发已有入场时间线；不得在门上另加“点击进入”“ENTER”等可见文字，除非用户重新要求。
5. 保留底部“夏瑶雯 / 个人档案”入口文字。
6. 内页可提供“再次入场”，返回入口并重新播放时间线。

## 内页背景动态
背景必须能被肉眼看出在动，可使用：
- 本地视频背景并提供静态兜底；
- 本地 Lottie JSON，置于内容后方；
- GSAP 或 CSS transform，让光带、颗粒、轨迹、液态形状或抽象物体明显位移、旋转、缩放和漂浮。
动画优先使用 `transform`、`opacity`，不能遮挡文字。配色遵循用户确认，避免默认青蓝、荧光粉和模板感。尊重 `prefers-reduced-motion`。

## 本项目的可复刻实现配方

要复现本项目当前版本，不能只读取文字规范，还必须读取仓库中的源码和资源：

- 依赖：React 18、Vite、TypeScript、`lottie-web`；当前项目也保留 Tailwind 配置。
- 入口实现位于 `src/main.tsx` 的 `gate` 分支；`videoUrl` 是正式花园门视频，点击状态为 `opening`，约 3200ms 后切换 `entered`。
- 入口结构使用 `.gate-video`、`.gate-shade`、`.gate-door`、`.sandfall` 和 `.gold-flash`；不要将它替换为 `.gate-ambient`、`.gate-frame`、`.door-panel` 或 `.entry-flash` 纯 CSS 门方案。
- 当前门的视频画面与入场时间线是确认版本；不要在门上再加可见提示文字或覆盖门面。
- 内页动态背景必须同时使用或等价替代：`public/microvisuals-bg.mp4` 的本地循环视频、`.portfolio-liquid` 的大面积缓慢位移、`.portfolio-motion` 的可见粒子与轨迹、`public/heartbeat.json` 通过 `lottie-web` 挂载到 `.lottie-bg`。
- 背景动态的关键不是换颜色，而是让背景视频、液态光带、粒子和 Lottie 在页面中持续发生明显位移、旋转或缩放；内容层的 z-index 必须高于背景。
- 这些资源是本项目仓库内的实际资源：`microvisuals-bg.mp4` 为项目使用的本地动态背景视频，`heartbeat.json` 为用户提供的 Lottie 动画文件。它们不是由 Skill 文字自动生成的，也不是外部开源门动画模板。
- 如果资源没有随 Skill 一起提供，AI 只能按照配方重写近似效果，不能保证产生一模一样的视频画面。因此交付给其他 AI 时，应同时提供整个仓库或至少提供 `src/`、`public/`、`package.json` 和本 Skill。

## 复刻提示词

“请先读取本 Skill 和仓库源码。不要先改个人资料。先向用户确认姓名、网站目的、栏目、项目数据、公开联系方式、颜色、字体、图片、动效和设备范围，得到确认后再实施。保留现有花园黑门视频入口、个人主页内容和网格排版。入口使用 `src/main.tsx` 的 `videoUrl` 与 `.gate-video`，不要替换成纯 CSS 门，不要在门上加可见‘点击进入’或遮罩；底部‘夏瑶雯 / 个人档案’保留。主页背景读取 `public/microvisuals-bg.mp4`、`public/heartbeat.json`，并让液态光带、粒子、轨迹和 Lottie 产生肉眼可见的 transform 动画。完成后运行 `npm run build`，检查敏感信息，再测试桌面、手机和微信内置浏览器。”

## 复刻边界与交付物

这份 Skill 是实施规范，不是可独立生成像素级成品的素材包。把 **Skill、完整仓库、`package.json` 与锁文件、`src/`、`public/`、已部署的 `assets/`** 一起交给另一款 AI，才可以复刻当前的页面结构、文案、样式和动画逻辑。

花园黑门入口视频目前由 `src/main.tsx` 内的外部 CloudFront `videoUrl` 提供，因此还需要该地址在当时可访问，或由用户提供同等替代视频。`microvisuals-bg.mp4` 与 `heartbeat.json` 则是仓库内资源。不得承诺仅凭文字 Skill 就能在外部视频失效时复刻完全相同的门画面；替代视频会改变视觉结果。

## 可直接交给其他 AI 的复刻任务

以下文字是本项目的执行提示词，不是抽象的风格描述。使用时必须同时提供完整仓库和资源，并要求 AI 先阅读源码再修改：

```text
请在现有 React 18 + TypeScript + Vite 项目上复刻一个轻盈、优雅、编辑感的个人作品集。先读取 SKILL-web-optimization.md、src/main.tsx、src/styles.css、public/microvisuals-bg.mp4、public/heartbeat.json 和 package.json。不要创建简化版，不要删除已有个人资料，不要用占位文案替换真实内容。

【入口门与花园】
入口必须使用花园背景中的真实黑色单扇门视频，视频地址为：
https://d8j0ntlcm91z4.cloudfront.net/user_38xzZboKViGWJOttwIXH07lWA1P/hf_20260819_212700_3bb9329b-5c50-4257-a09b-ca85cf3654a3.mp4
视频全屏 object-fit: cover，autoplay、muted、loop、playsInline。花园、花朵、黑色门必须来自这个视频画面；不要改成 CSS 画门、透明门、两扇左右开门、游戏加载界面或另一张静态图。不要叠加门把手、星星、蝴蝶、流沙、金色光圈、白色按钮、ENTER、WELCOME 或“点击进入”等可见元素。底部只保留“夏瑶雯 / 个人档案”。点击视频门所在的透明命中区域后，进入 opening 状态；约 3200ms 后切换到个人主页。入口必须适配手机和微信内置浏览器，不能因视频未自动播放而显示黑屏；视频失败时要有不遮挡内容的暗色静态兜底。

【内页背景动态】
进入个人主页后，背景不能是静态截图，也不能只改变颜色。使用 public/microvisuals-bg.mp4 作为本地循环背景视频，preload="auto"、autoplay、muted、loop、playsInline，并使用明显但克制的 transform 位移/缩放让画面持续运动。叠加 .portfolio-liquid 的大面积低饱和液态光带，使用 transform、scale、rotate 和 opacity 做可见的缓慢漂移；使用 public/heartbeat.json 通过 lottie-web 挂载到 .lottie-bg 并循环播放。背景视频、液态光带、Lottie 的 z-index 必须低于文字，不能遮挡交互。动画应在桌面上肉眼可见，在手机上降低尺寸和透明度但不能完全消失；prefers-reduced-motion 时降低或停止非必要动画。浅色背景优先，避免青蓝主色、荧光粉和高饱和模板渐变。

【页面内容与排版】
保留四个栏目：关于我、项目经历、工作方式、联系我。使用完整真实文案和数据。姓名统一写“夏瑶雯”，工具统一写 Pandas，平台统一写“抖音”。桌面采用对齐的网格，不做阶梯式错位；内容占据合理画布，不把桌面页面压缩成手机窄栏。标题使用优雅细衬线字体，正文和标签保证足够对比度与可读字号。项目经历至少保留 4 项：北辰青年 / 校园行动合伙人、内容运营 / 双平台数据复盘、数据分析学习项目、个人 AI 工具探索。工作方式固定为“先找问题 / 再做结构 / 最后交付”，每项都写动作、证据和可复盘结果。

【验证】
修改后运行 npm run build。确认构建入口引用最新 dist 资源，检查没有旧版 HTML、旧资源哈希、localhost、127.0.0.1、本机路径、令牌或密钥。用桌面、手机和微信内置浏览器检查：入口花园门可见、点击后可进入、内页不是黑屏、背景视频实际播放、液态光带或 Lottie 实际运动、四个栏目可打开、再次入场可用。
```

## 门与背景的实现验收清单

- 门的视觉来源只能是指定 CloudFront 视频；外部视频不可访问时，应提示需要用户提供替代视频，而不是擅自画一扇新门。
- 点击后必须有确定的状态切换和一次性计时器，不能因为 React 重渲染重复触发多次跳转。
- 内页视频元素必须存在 `autoPlay muted loop playsInline preload="auto"`；CSS 必须包含非颜色变化的 `transform` 动画。
- 进入页面后在至少 5 秒内能观察到背景位移、缩放、旋转、粒子或 Lottie 的实际变化；仅改变色相、透明度或渐变颜色不算动态背景。
- 视频加载慢时必须仍能看到页面内容和轻量 CSS 动态；视频加载完成后不得把文字压暗或遮挡。
- 在微信内置浏览器测试时，不依赖 hover、WebGL 或用户手势才能显示首屏；关键文字和入口不应只存在于视频帧中。

## 内容结构
默认栏目为“关于我、项目经历、工作方式、联系我”，可按用户回答增删。

### 关于我
使用姓名、英文导语、个人定位、教育背景、真实经历、成果和统计数据；可加入图片或视觉卡片，但不得重复使用同一人物头像。

### 项目经历
每项写清背景、本人负责什么、使用什么工具、可见产出、结果或当前阶段；练习项目诚实标记为学习项目。

### 工作方式
固定三步且同一基线排列：先找问题（从目标开始拆解）；再做结构（把混乱变成路径）；最后交付（让结果能被使用）。每一步写动作、项目证据和可复盘结果。

### 联系我
姓名、公开身份、联系方式和记忆点文字放在明确焦点中。联系方式使用足够字号、高对比度和细衬线斜体，不能竖排或挤在角落。

## 已确认资料示例
只有在用户确认仍有效时才能使用：夏瑶雯；西安明德理工学院数据科学与大数据技术专业；北辰青年项目中参与调研、整理 93 份有效问卷，并参与报告、汇报 PPT 和招募长海报；抖音最高播放量 12.4 万、点赞 4.1 万；小红书最高播放量 5.6 万、点赞 1.5 万；Excel、SQL、Pandas、数据清洗、指标拆解、基础可视化；知识库、Prompt、本地工具和工作流设计探索。

术语必须写作 `Pandas`，平台名称必须写作“抖音”。不得把 Pandas 翻译成“熊猫”，不得写错“抖音”。

## 工程与安全
- 本仓库实际使用的开源依赖：React 18、React DOM、Vite、TypeScript、Tailwind CSS 配置、`lottie-web` 和 `lucide-react`。`Flower2` 来自 `lucide-react`，只用于页面装饰图标。
- 本仓库实际使用的动画实现：入口和主页背景主要由 `src/styles.css` 的 CSS `transform`、`opacity`、`perspective`、`@keyframes` 实现，Lottie 通过 `lottie-web` 挂载 `public/heartbeat.json`，主页动态背景使用仓库内的 `microvisuals-bg.mp4`。
- 当前版本没有安装或调用 GSAP，也没有导入某个开源门动画模板。以后若使用 GSAP，必须把依赖、调用位置、版本和来源记录在这里，并先验证移动端性能。
- `microvisuals-bg.mp4` 是本仓库内的动态视频资源，`heartbeat.json` 是随项目保存的 Lottie 资源；AI 只有在取得整个仓库及这些资源时，才能复现相同画面。只拿 Skill 文档不能凭空生成相同视频。
- React 项目优先使用现有 GSAP、Lottie 和依赖，不重复引入 UI 套件。
- 修改前检查入口和端口，避免旧服务或缓存。
- 提交前运行 `npm run build`，并全文检查敏感信息。
- 禁止提交 `127.0.0.1`、`localhost`、局域网 IP、Windows 路径、临时路径、令牌、密钥和不应公开的个人信息。
- 不提交 `node_modules`、临时截图、构建缓存和无关实验文件。
- GitHub Pages 资源使用站点根路径，不写本机路径。
- 发布后检查桌面端、手机端和微信内置浏览器：入口不黑屏、动态资源加载、返回和再次入场可用。
